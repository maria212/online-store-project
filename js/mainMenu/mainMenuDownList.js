class mainMenuDownList {
    constructor(source) {
        this.source = source;
        this._init();
    }
    _init() {
        /* return `<li><a href="${this.href}">${this.title}</a></li>`; */
    }

}